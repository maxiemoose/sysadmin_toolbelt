##
## author: Karol Tomczuk <karol.tomczuk@zerodowntime.pl>
##

---
- name: take sudo privileges from sudo user
  user:
    name={{ item.user }}
    groups=
  with_file:
    - users_list
  when:
    ansible_distribution == 'Ubuntu' and
    ansible_distribution_release == 'xenial'
